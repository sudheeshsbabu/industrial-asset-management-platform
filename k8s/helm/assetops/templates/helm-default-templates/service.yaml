apiVersion: v1
kind: Service
metadata:
  name: {{ include "assetops.fullname" . }}
  labels:
    {{- include "assetops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "assetops.selectorLabels" . | nindent 4 }}
